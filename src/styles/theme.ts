import { PaletteType } from '@material-ui/core'

export const light = {
  palette: {
    type: 'light' as PaletteType,
    primary: {
      light: '#a6d4fa',
      main: '#90caf9',
      dark: '#648dae'
    }
  }
}

export const dark = {
  palette: {
    type: 'dark' as PaletteType
  }
}
